
const width = 150;
const height = 100;

const reStart = -2;
const reEnd = 1;
const imStart = -1;
const imEnd = 1;
const reWidth = reEnd - reStart;
const imWidth = imEnd - imStart;

const maxIter = 72;

export { 
    width, height, reStart, reEnd, imStart, imEnd, maxIter, reWidth, imWidth 
}

